import { PUppercasePipe } from './p-uppercase.pipe';

describe('PUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new PUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
